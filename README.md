# Infra for deploying ARC

This repo uses a private package as this frustrates our build process we've wrapped this piece of infra in a separate repo from mijn-servies.

## Building 
Uses a private package so to build run
```
set -a                 
source .env
set +a
```
